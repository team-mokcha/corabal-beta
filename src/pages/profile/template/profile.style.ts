import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  profileContainer: {
    marginTop: 40,
    display: "flex",
    justifyContent: "center"
  },
  profileOrganismsWrapper: {
    marginLeft: 36,
    marginRight: 36
  }
});

export default styles;

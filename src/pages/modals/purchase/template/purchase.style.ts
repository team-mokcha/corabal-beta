import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  bodyWrapper: {
    textAlign: "center",
    alignItems: "center",
    marginTop: 52
  },
  callCatImage: {
    marginTop: -8,
    marginBottom: 50
  }
});

export default styles;

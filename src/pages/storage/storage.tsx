import React, { ReactElement, useState } from "react";
import firebase from "firebase";
import Template from "./template";

const Storage = (): ReactElement => {
  return <Template />;
};

export default Storage;

import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  pointFont: {
    fontWeight: "bold",
    color: "#2B8CFF"
  },
  recordContainer: {
    marginTop: 24,
    marginLeft: 24,
    marginRight: 24,
    marginBottom: 40
  },
  thisMonthFont: {
    fontSize: 15,
    fontWeight: "500",
    marginBottom: -30
  },
  totalCupsFont: {
    fontSize: 24
  }
});

export default styles;

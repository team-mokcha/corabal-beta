import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  confirmMessageFont: {
    marginTop: -40,
    marginBottom: 24,
    color: "#000000",
    fontSize: 14,
    lineHeight: 20.27
  }
});

export default styles;

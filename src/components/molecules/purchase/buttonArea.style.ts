import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  buttonWrapper: {
    display: "flex",
    flexDirection: "row",
    alignItems: "center"
  },
  button: {
    minWidth: 136,
    height: 48
  }
});

export default styles;

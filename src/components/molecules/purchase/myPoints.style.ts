import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  pointsWrapper: {
    display: "flex",
    flexDirection: "row",
    alignItems: "center"
  },
  myPointsFont: {
    marginTop: 2,
    marginRight: 8,
    color: "#000000",
    fontSize: 12,
    lineHeight: 14.06
  }
});

export default styles;

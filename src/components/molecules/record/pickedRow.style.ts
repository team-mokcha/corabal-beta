import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    marginBottom: 32
  },
  cupIcon: {
    marginBottom: 32
  },
  pickedIcon: {
    marginBottom: 32
  },
  deleteIcon: {
    marginBottom: 32
  }
});

export default styles;

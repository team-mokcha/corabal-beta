import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  banner: {
    resizeMode: "contain",
    height: 170,
    alignSelf: "center",
    // marginLeft: 24,
    // marginRight: 24,
    marginBottom: 60
  }
});

export default styles;

import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  aimContainer: {
    marginLeft: 24,
    marginTop: 40
  },
  aimSetting: {
    fontSize: 14
  }
});

export default styles;

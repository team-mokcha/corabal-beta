import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  aim: {
    fontSize: 18,
    fontWeight: "500",
    marginBottom: 8
  },
  aimStrong: {
    fontSize: 18,
    color: "#2B8CFF"
  }
});

export default styles;

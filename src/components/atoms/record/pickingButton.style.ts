import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  optionAlign: {
    marginRight: 30,
    alignItems: "center"
  },
  optionImg: {
    width: 27,
    height: 27,
    marginBottom: 4
  },
  unPickedOption: {
    color: "#CECECE"
  }
});

export default styles;
